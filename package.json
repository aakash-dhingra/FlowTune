{
  "name": "flowtune",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Music playlist management with AI",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"cd client && npm run dev\" \"cd server && npm run dev\"",
    "build": "npm --prefix server run prisma:generate && npm --prefix client run build && npm --prefix server run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
